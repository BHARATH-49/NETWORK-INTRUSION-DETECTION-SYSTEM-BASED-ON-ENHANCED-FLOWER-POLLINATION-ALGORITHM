# Results

The original notebook prints classification results directly during execution rather than writing them to separate result files.

This directory is reserved for exported classification reports, tables, plots, or experiment logs if the project is later extended.

Do not treat a newly generated result as an original historical result unless it is explicitly identified as a re-run.
